export const raid = {
	name: '!raid',
	description: 'Handles the !raid command',
	execute: async (message) => {
		// Your raid logic here
		if (false) {
			await message.reply('Response for condition 1');
		} else if (false) {
			await message.reply('Response for condition 2');
		} else {
			await message.reply('Default response');
		}
	},
};
